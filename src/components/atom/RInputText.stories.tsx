import RInputText from "./RInputText";

const meta = {
    title: 'atom/RInputText',
    component: RInputText,
    tags: ['autodocs'],
    argType: {},
}

export default meta;
export const Primary = {
    args: {},
}